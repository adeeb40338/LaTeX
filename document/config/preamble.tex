% --------------------------------------------------
% SYNTAX CHECKING
% --------------------------------------------------
% 
% To make LaTeX check the document for proper syntax usage
% use the command '\syntaxonly'. This however will prevent the
% document from being typeset. I.e. it will result in the document
% only being checked and not made into a PDF.
%

% Enable the syntax checking package.

% \usepackage{syntonly}

% Carry out syntax checking.

% \syntaxonly

% --------------------------------------------------
% DECLARE THE PACKAGES TO BE USED
% --------------------------------------------------

% Enable long comments.

\usepackage{verbatim}

% Enable mathematical equations.

\usepackage{amsmath}

% --------------------------------------------------
% RENAME ABSTRACT TITLE
% --------------------------------------------------

% \renewcommand{\abstractname}{Executive Summary}

